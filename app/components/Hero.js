import Carousel from './Carousel';
import AnimatedTitle from './AnimatedTitle';

const Hero = () => {
  return (
    <div>
      <AnimatedTitle />
      <Carousel />
    </div>
  );
};

export default Hero;